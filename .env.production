# Öffentliche Supabase-Werte (geteiltes Projekt "foerderrechner", eigener
# Tabellen-/Funktionsname sanierung_*) — kein Geheimnis, committet wie bei
# den Schwester-Tools, damit Vercel sie ohne manuelle Dashboard-Konfiguration
# übernimmt. Der publishable Key ist durch RLS geschützt (Tabelle ohne
# Policies gesperrt) und darf ins Client-Bundle.
#
# Secrets (IP_SALT, REQUEST_TOKEN_SECRET) liegen ausschließlich als Vercel
# Environment Variables, nie in dieser Datei. Ein Service-Role-Key wird für
# dieses Projekt nicht benötigt (siehe Rate-Limit-RPC, SECURITY DEFINER).
NEXT_PUBLIC_SUPABASE_URL=https://gsmuqvjyfjtjbbxqmfgt.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_o38GVg2VcyPJr2pqROPMiQ_neQBsqu1
ALLOWED_ORIGINS=https://sanierungsrechner.energetisiert.de,https://energetisiert.de
